# Render Deployment Checklist

## ✅ Files Ready for Deployment

### Required Files (Include in Git):
- ✅ `app.py` - Main application
- ✅ `requirements.txt` - Dependencies (cleaned up)
- ✅ `render.yaml` - Render configuration
- ✅ `Procfile` - Start command
- ✅ `runtime.txt` - Python version
- ✅ `templates/` - All HTML templates
  - `base.html`
  - `dashboard.html`
- ✅ `static/` - CSS and static files
  - `css/style.css`
- ✅ `.gitignore` - Excludes unnecessary files

### Files to Exclude (Already in .gitignore):
- ❌ `.env` - Use Render environment variables instead
- ❌ `__pycache__/` - Python cache
- ❌ `*.exe` - Windows executables
- ❌ `*.bat` - Batch files
- ❌ `aga_cie3/` - Old/unused folder (optional to remove)

## 🔑 Environment Variables to Set in Render

Go to Render Dashboard → Your Service → Environment → Add:

1. **GEMINI_API_KEY**
   - Value: `AIzaSyBSdU11veHbp_9jlVYDn8wIUcREVBlTC1Y`

2. **OPENROUTER_API_KEY**
   - Value: `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`

3. **SECRET_KEY** (Optional - Auto-generated by Render)
   - Or set your own secure random string

4. **FLASK_DEBUG** (Optional)
   - Value: `False` (for production)

## 📦 Dependencies Included

The `requirements.txt` includes only necessary packages:
- Flask (web framework)
- vaderSentiment (tone analysis)
- requests (API calls)
- gunicorn (production server)
- transformers (ML models)
- torch (ML backend)
- accelerate (ML optimization)
- google-generativeai (Gemini SDK - optional)
- python-dotenv (environment variables)

## 🚀 Deployment Steps

1. **Commit all files:**
   ```bash
   git add .
   git commit -m "Ready for Render deployment"
   git push origin main
   ```

2. **On Render:**
   - New → Web Service
   - Connect GitHub repo
   - Render will auto-detect `render.yaml`
   - Set environment variables (see above)
   - Deploy!

3. **Verify:**
   - Check health endpoint: `https://your-app.onrender.com/health`
   - Test tone analysis with sample email
   - Verify Gemini and OpenRouter APIs work

## ⚠️ Important Notes

- Never commit `.env` file (already in .gitignore)
- API keys must be set in Render Dashboard, not in code
- Free tier has cold starts (15-30 seconds first load)
- Gmail IMAP may not work on free tier

## 📝 Quick Reference

**Start Command:** `gunicorn app:app`  
**Build Command:** `pip install -r requirements.txt`  
**Python Version:** 3.11.6  
**Port:** Auto-set by Render (use `PORT` env var)

