# Render Deployment Guide

## Quick Deploy Steps

### 1. Push to GitHub
```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

### 2. Deploy on Render

1. Go to [render.com](https://render.com)
2. Sign up/Login with GitHub
3. Click "New" → "Web Service"
4. Connect your GitHub repository
5. Render will auto-detect `render.yaml` and use those settings

### 3. Set Environment Variables (REQUIRED)

Go to your service → Environment → Add these variables:

**Required API Keys:**
- `GEMINI_API_KEY` = `AIzaSyBSdU11veHbp_9jlVYDn8wIUcREVBlTC1Y`
- `OPENROUTER_API_KEY` = `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`

**Optional:**
- `SECRET_KEY` = (Auto-generated by Render, or set your own)
- `FLASK_DEBUG` = `False` (for production)

### 4. Deploy!

Render will automatically:
- Install dependencies from `requirements.txt`
- Start the app with `gunicorn app:app`
- Your app will be available at: `https://your-app-name.onrender.com`

## File Structure for Deployment

**Required Files:**
- `app.py` - Main Flask application
- `requirements.txt` - Python dependencies
- `render.yaml` - Render configuration (optional but recommended)
- `Procfile` - Start command (optional, render.yaml takes precedence)
- `runtime.txt` - Python version
- `templates/` - HTML templates
- `static/` - CSS and static files

**Not Needed (excluded via .gitignore):**
- `.env` - Use Render's environment variables instead
- `__pycache__/` - Python cache
- `*.exe`, `*.bat` - Windows-specific files

## Environment Variables in Render

All API keys should be set in Render Dashboard → Your Service → Environment:

1. Click "Add Environment Variable"
2. Add each key:
   - Key: `GEMINI_API_KEY`
   - Value: Your Gemini API key
3. Repeat for `OPENROUTER_API_KEY`
4. Click "Save Changes"
5. Render will automatically redeploy

## Testing Your Deployment

1. Visit your Render URL
2. Paste email text in the textbox
3. Select an AI model (Gemini or OpenRouter)
4. Click "Analyze Tone"
5. Verify suggestions appear

## Troubleshooting

**Build fails:**
- Check `requirements.txt` has all dependencies
- Verify Python version in `runtime.txt` matches Render's supported versions

**App crashes:**
- Check Render logs for errors
- Verify all environment variables are set
- Ensure API keys are valid

**API not working:**
- Verify API keys are set in Render environment variables
- Check logs for API errors
- Test API keys locally first

**Slow first load:**
- Normal on Render free tier (cold starts take 15-30 seconds)
- Subsequent requests are faster

## Notes

- ⚠️ Gmail IMAP feature may not work on Render's free tier
- ✅ Core tone analysis works perfectly
- 🔒 Never commit API keys to GitHub (use Render environment variables)
- 📝 The `.env` file is gitignored - use Render's environment variables instead

